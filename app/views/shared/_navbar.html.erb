<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to image_tag("logo.png", class: "logo"), root_path %>

  <!-- Container wrapper -->
  <div class="container">
    <!-- Search form -->
   <%= render "search/form" %>
    <div>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link d-flex flex-column text-center" %>
        </li>
          <% if session[:user_id].present? %>
            <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-primary" %>
          <% end %>
      </ul>
    </div>    
    <!-- Collapsible wrapper -->
  </div>
  <!-- Container wrapper -->
  <div class="text-white">
  <p>Hello! <%= logged_user.username%></p>
    <% if logged_user.avatar.attached? %>
      <%= link_to image_tag(url_for(logged_user.avatar), size: "150x150", class: "img-circle"), edit_user_path(logged_user), method: :get%>
    <% else %>
      <%= link_to image_tag("default_avatar.png", size: "150x150"), edit_user_path(logged_user), method: :get %>
    <% end %>
    <%= link_to "Edit user", edit_user_path(logged_user), method: :get, class: "btn btn-success" %>
    <%= link_to "Invite", invite_path, method: :get, class: "btn btn-info" %>
  </div>
</nav>
<!-- Navbar -->
