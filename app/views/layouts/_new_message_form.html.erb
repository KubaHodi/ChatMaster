<%= form_with(model: [ @message, @room ], remote: true, class: "d-flex") do |f| %>
<%= f.text_field :content, id: "text-message", class: "form-control", autocomplete: "off" %>
<%= f.submit data: { disable_with: false }, class: "btn btn-primary" %>
<% end %>